package factory;

public class BarbarianUnit extends BasicUnit {
	public BarbarianUnit() {
		setName("Barbarian");
		setDescription("An AI controlled unit typially used for early assaults against pretty muh defenceless players");
		setDamage(59.9);
		setUtilityRate(3);

	}

}
